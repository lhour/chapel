# 3D 场景建设 Skill / Workflow（供 Agent 阅读）

> 适用范围：程序化生成 3D 场景（参考栈：Bevy 0.19 + Rust；方法论适用于任何引擎）。
> 核心原则：**规划先行 → 坐标推导 → 确定性变化 → 渲染验证闭环 → 迭代交付**。
> 参考实现：本仓库（chapel，半塌小教堂）与 warbell 项目（视觉管线来源）。

---

## 第 0 阶段：信息收集（写任何代码之前，必须完成）

**用户提供信息不全时，逐项主动询问，禁止自行假设关键信息。** 以下 9 项按重要性排序：

| # | 问题 | 默认值（仅当用户明确说"随便/你定"时才可使用） |
|---|------|------|
| 1 | **引擎与版本** | Bevy 0.19（需 rustc ≥1.95；沙箱 rustc <1.95 时降级 0.18，见附录 B API 差异） |
| 2 | **场景主体** | 无默认，必问（建筑？废墟？自然景观？室内？） |
| 3 | **风格参考** | 低多边形 + 顶点色（warbell 风）；用户给参考项目则优先参考 |
| 4 | **尺度单位** | 真实米制（1 单位 = 1m），人高约 1.7m 作基准 |
| 5 | **破损程度** | 必问：完好 / 轻损 / 半毁 / 全毁，且**每个部分分别问**（哪面墙塌、塌多少） |
| 6 | **时间 / 天气 / 氛围** | 正午暖阳 + 轻雾 |
| 7 | **相机** | 轨道相机（自动慢转） |
| 8 | **用途与性能预算** | 截图演示；合批到少量 draw call |
| 9 | **参照物** | 树、草、碎石（强烈建议保留，防"玩具感"） |

**询问技巧**：一次最多问 3-4 个问题（用选项形式），不要连环轰炸。用户说"搭个破教堂看看"这种模糊指令时，至少要补问：破损程度分布、风格参考、尺度。

---

## 第 1 阶段：建造规划文档（先文档，后代码，等用户确认）

产出以下规划并展示给用户确认，**确认前不写实现代码**：

### 1.1 平面图（ASCII 俯视示意）
```
        北墙（塌20%）
   ┌─────────────────┐
   │                 │  后殿(半圆)
西 │    中殿          │ ▓▓
墙 │  (10m × 16m)    │▓▓▓▓
塌 │                 │ ▓▓
45%│      ▣ 塔楼     │
   └─────────────────┘
        南墙（塌35%）
```

### 1.2 常量表（所有尺寸必须是常量，禁止魔法数字）
```rust
pub const NAVE_W: f32 = 10.0;  // 主体宽
pub const NAVE_L: f32 = 16.0;  // 主体长
pub const WALL_H: f32 = 6.0;   // 墙高
pub const WALL_T: f32 = 0.8;   // 墙厚
pub const BLOCK_W: f32 = 1.0;  // 砖宽
pub const BLOCK_H: f32 = 0.5;  // 每皮砖高
```

### 1.3 破损规划表（每面墙单独指定）
| 部件 | 损伤等级(0~1) | 破损类型 | 说明 |
|------|------|------|------|
| 西墙 | 0.45 | 顶部缺失 | 最惨，与塔楼坍塌呼应 |
| 南墙 | 0.35 | 顶部缺失+中部洞 | |
| 东墙 | 0.25 | 顶部轻微 | |
| 北墙 | 0.20 | 顶部轻微 | |
| 塔楼 | 0.60 | 冠部塌陷 | 只剩残缺齿状 |

### 1.4 色板（每种材质 ≥3 档明暗，低饱和）
```rust
pub const STONE_LIGHT: u32 = 0xb9b5a8; // 风化石·亮
pub const STONE:       u32 = 0x9a968a; // 风化石·中
pub const STONE_DARK:  u32 = 0x6f6b62; // 风化石·暗
```

### 1.5 参照物清单 + 光照参数（与 warbell 视觉管线对齐，禁止留空）

**参照物（强烈建议保留，消除"玩具感"= 没有参照物）**：

| 参照物 | 作用 | 建议密度 / 数量 |
|--------|------|----------------|
| 独立树（树干 + 2 层锥形冠） | 提供尺度锚点（树高≈5m → 墙面能心算出高度） | 场景外围 8~12 棵，呈不规则环形 |
| 草丛 / 草簇（小方块/扁块） | 填充地面单调感，打破"平面=游戏地图"感 | 30~60 块，避开建筑入口 ±5m |
| 碎石（小立方体，随机旋转+暗色） | 物理直觉：有坍塌就该有碎块在对应墙脚 | 20~40 块，集中在破损最严重的墙面 ±2m 内 |
| 铺地石板碎片（薄 slab，随机倾斜） | 表明"此处曾经是广场/步道"，增加年代感 | 10~15 块，在入口扇形区域 |

**光照参数（warbell 风格正午暖阳，数值直接抄）**：

```rust
// 平行光 —— 暖阳 + 长阴影
DirectionalLight {
    illuminance: 40_000.0,                         // 标准正午，10000=阴天，100000=赤道烈日
    color: Color::srgb(1.0, 0.96, 0.88),           // 偏黄 0.96/0.88，不要纯白
    shadow_maps_enabled: true,                      // 0.19 字段名，没阴影=一切漂在空中
    ..default()
}
// Transform 角度：约从西南-西北方向射下，让东侧/北侧墙面落阴
// Quat::from_euler(EulerRot::XYZ, -0.9, -0.9, -0.3)

// 环境光 —— 冷色补光，中和暖太阳的黄
GlobalAmbientLight {
    color: Color::srgb(0.62, 0.68, 0.8),           // 浅蓝紫，0.68/0.8 是天空散射色
    brightness: 900.0,                              // 900 以下会暗，1200 以上发灰
    ..default()
}

// 距离雾 —— 暖灰调（陷阱：千万别用天空蓝！）
DistanceFog {
    color: Color::srgb(0.85, 0.80, 0.66),           // 奶油雾 0x85_80_66 类
    directional_light_color: Color::srgb(0.95, 0.92, 0.84),
    directional_light_exponent: 6.0,                 // 越大=阳光散射越窄
    falloff: FogFalloff::ExponentialSquared { density: 0.009 },  // 截图场景默认 0.009
    ..default()
}

// 天空清色（ClearColor）—— 日间浅蓝，比雾色亮 1 档
ClearColor(Color::srgb(0.70, 0.82, 0.93))
```

---

## 第 2 阶段：建模规则（消除"对不齐/不闭合"）

### 2.1 坐标必须由公式推导
```
位置 = 层号 × BLOCK_H + BLOCK_H/2   （竖向）
     = 列号 × BLOCK_W + 错缝偏移      （横向）
墙体定位 = ±NAVE_W/2、±NAVE_L/2（从角点推导，角部天然闭合）
```
**禁止出现手写小数坐标**（如 `x=5.1`）——这是砖块错位的第一根因。

### 2.2 错缝（running bond）
```rust
let off = if cy % 2 == 0 { 0.0 } else { BLOCK_W * 0.5 }; // 奇数层偏移半砖
```
砖块间留缝：块体缩放至 0.92 模拟灰缝，视觉上区分单块砖。

### 2.3 角部闭合
- 相邻墙共用同一角点坐标（由 `±W/2, ±L/2` 推导）
- 方形塔楼四角补角柱（用暗色石块），堵住墙体交汇处的缝

### 2.4 确定性变化（替代随机数）
```rust
fn hash2(x: i32, y: i32) -> f32 { /* 确定性哈希，可复现 */ }
```
- 同一构建永远产出同一结果 → 出错可定位、可回归
- 用途：单块砖选色（三档）、缺块判定、碎砖散布位置

### 2.5 破坏的物理直觉
```
缺损集中在墙体【顶部】，越往上缺口越大（真实废墟因重心坍塌）
碎砖散布在【对应墙脚】±2m 内（像塌下来的，不是天上撒的）
```
```rust
let miss = if r < 0.5 && top_frac > 1.0 - drop { true } else { r < 0.05 };
```

---

## 第 3 阶段：材质与色彩（消除"玩具感"的第一战场）

1. **低饱和**：所有颜色 HSL 饱和度 < 25%；灰调做旧色（`0xb9b5a8` 而非 `0xcccccc` 更非 `0xffffff`）
2. **同材质多档**：每材质 3 档明暗 + 每块随机选择 → 打破"颜色统一=塑料感"
3. **合批配方**：全场景共用一个白色 `StandardMaterial`，颜色全部走顶点色 → 自动合批
   ```
   图元 → tinted(顶点色) → merge → duplicate_vertices → compute_flat_normals
   ```
4. **平面着色**（flat shading）：低多边形风格的立体感来源
5. **每块砖亮度微扰动**（±10%）：叠加在色档之上，细看有手工感

---

## 第 4 阶段：光照与渲染管线（消除"玩具感"的第二战场）

标准管线（缺任何一环都会翻车）。**以下为 Bevy 0.19 写法**（与 warbell `src/scene.rs` 对齐）：

```rust
use bevy::camera::{Exposure, Hdr};      // 0.19: Hdr 是独立组件，在 camera 模块下
use bevy::pbr::{DistanceFog, FogFalloff};
use bevy::core_pipeline::tonemapping::Tonemapping;

// 相机：Hdr 是独立组件（NOT Camera { hdr: true } — 0.17 以前老写法已移除）
(
    Camera3d::default(),
    Hdr,
    Exposure::INDOOR,            // 或自定义，无此项=室内暗室外过曝
    Tonemapping::AgX,            // 色调映射：无此项=过曝发白或死黑
)

// 平行光：暖色太阳 + 阴影
DirectionalLight {
    illuminance: 40_000.0,
    color: Color::srgb(1.0, 0.96, 0.88),  // 暖阳
    shadow_maps_enabled: true,             // 0.19: 字段改名，不是 shadows_enabled
    ..default()
}

// 环境光：作为 Resource 插入（不是组件！）
.insert_resource(GlobalAmbientLight {
    color: Color::srgb(0.62, 0.68, 0.8),
    brightness: 900.0,
    ..default()
})

// 距离雾：暖灰调，NOT 天空蓝（雾色陷阱见下文）
DistanceFog {
    color: Color::srgb(0.85, 0.80, 0.66), // 暖雾：阳光大气感，别用天空蓝
    directional_light_color: Color::srgb(0.95, 0.92, 0.84),
    directional_light_exponent: 6.0,
    falloff: FogFalloff::ExponentialSquared { density: 0.009 },
    ..default()
}
```

**雾色陷阱**：雾不要用天空蓝（整帧泛白），用暖灰调（`0x85_80_66` 类），读作"阳光大气"。

---

## 第 5 阶段：验证闭环（每轮修改后必须执行，最重要）

### 5.1 渲染截图
- 无 GPU 沙箱：llvmpipe 软渲染（`LIBGL_ALWAYS_SOFTWARE=1`），跑 N 帧后存 PNG
- 有 GPU：直接截帧

### 5.2 自查（按能力分支）

**Agent 有视觉能力** → 直接看图检查下列清单。

**Agent 无视觉能力** → **首选内存截面扫描（方案 D）**，几何断言（A）作为快速前置检查；像素/深度探针（B/C）仅在截面方案不可行时使用。方案优先级：**D > A > C > B**。

**A. 几何断言（构建期，最快，不需要渲染）**
```
- 闭合检查：四墙角点坐标两两相等（误差 < 0.01）
- 对齐检查：每块砖中心 ∈ {i×BLOCK_W} 网格
- 穿模检查：所有 cuboid AABB 两两求交（排除合法相邻）
- 悬空检查：每块砖下方有支撑（同列下一层存在 或 落地）
```

**B. 像素探针（读帧缓冲内存，验证渲染结果）**
```
1. 把待验证 3D 点（墙角、门洞、地面）投影到屏幕坐标
2. 读该像素颜色
3. 断言：
   [OK]   墙角A → 石色 #8a877c → 墙存在，闭合
   [FAIL] 墙角B → 天空蓝 #9ec6e8 → 该处无墙！不闭合
```

**C. 深度探针（比颜色更准）**
```
渲染 depth-only → 沿墙走向采样深度 → 连续=墙面完整，突变=有洞
```

**D. 内存截面扫描（首选方案，专为无视觉模型设计）**

> 原理：不依赖截图和像素读取，直接在**构建期**从建筑数据生成截面图，把 3D 结构降维成 2D 占据网格，输出为文本矩阵给模型阅读。模型读到的不是图片，是"数字化的建筑图纸"。

```
流程：
1. 构建期记录每块砖的中心坐标与尺寸（build 时同步写入一个 Vec<BlockInfo>）
2. 按需选取切面方向与粒度（见 D1/D2），生成占据网格
3. 占据网格用字符渲染：
   ▓ = 实体（砖块占据）
   · = 空隙（该有砖而没有 → 不闭合/破洞）
   × = 规划中的破损位（允许为空）
4. 模型直接读文本矩阵做断言
```

**D1. 切面方向（横 / 竖 / 斜，按检查目标选择）**

| 切法 | 定义 | 检查什么 | 适用时机 |
|------|------|------|------|
| **横截面**（俯视，y=h） | 固定高度水平切一刀，输出 长×宽 网格 | 平面布局：房间是否围合、墙体走向、门洞位置、塔楼与主体衔接 | 验证规划平面图的第一刀 |
| **竖截面**（立面，沿墙走向切） | 垂直平面沿墙轴切一刀，输出 沿向×高度 网格 | 墙体立面：闭合（两端列全 ▓）、破损分布、高度轮廓 | 每面墙各切一刀 |
| **竖截面**（横切墙身，垂直于墙轴） | 垂直于墙的平面切一刀，输出 厚度×高度 网格 | 墙厚一致性、灰缝饱满、内外皮对位 | 抽查典型位置 |
| **斜截面**（对角线切） | 沿建筑对角线方向斜切，输出 对角向×高度 网格 | **对角结构**：角部闭合（斜切必过角点）、非正交件（后殿半圆鼓座、斜撑、楼梯） | 正交截面查不到的对角问题 |
| **斜截面**（任意法向 n） | 平面方程 n·p = d，任意角度斜切 | 倾斜构件：破屋顶坡度、坍塌斜面、斜塔 | 有斜构件时必查 |
| **逐层截面** | 每皮砖一层水平切，输出多层俯视 | 错缝规则、悬空砖、层间对位 | 错缝/悬空专项检查 |

**斜截面原理**：占据判定不依赖轴对齐——把每个 BlockInfo 的 AABB 投影到切面坐标系（切面法向 n、面上基 u,v），在 uv 网格上做 box-raster 即可，与横竖截面同一套代码。任何方向的截面都能切出来。

**D2. 粒度分级（粗略 → 细致，逐级收窄）**

| 级别 | 分辨率 | 用途 | Token 成本 |
|------|------|------|------|
| **L0 概览** | 每格 = 4m（建筑整体缩到 ~20×20） | 看整体布局、大结构：主体/塔/后殿关系，一眼发现"整体歪了" | 极低 |
| **L1 标准** | 每格 = 1 块砖（BLOCK_W×BLOCK_H） | 常规检查：闭合、错缝、破损分布 | 中 |
| **L2 细查** | 每格 = 1/4 砖 | 灰缝饱满度、单砖级错位、穿模缝隙 | 高，仅对问题区域局部使用 |

**粗→细流程**（推荐）：先 L0 全景扫一遍确认大结构无误 → L1 逐墙/逐层标准检查 → 发现可疑区域（如某角列不齐）→ 只对该区域切 L2 细查。**不要一上来就全场景 L2**，token 爆炸且噪声淹没重点。

**D3. 依据场景类型调整切面策略**

| 场景类型 | 必切 | 可省略 | 原因 |
|------|------|------|------|
| 矩形主体建筑 | 横 y=1m、各墙竖切、逐层 | 斜切 | 正交结构横竖截面全覆盖 |
| 带圆后殿/塔楼 | 横多切几刀（y=0.5/3/5.5）、斜切过圆心 | — | 圆弧件只与斜截面相交出真实形态 |
| 有破/斜屋顶 | 沿屋脊斜截面、垂直屋脊竖切 | 逐层 | 屋顶坡度只有斜截面能量化 |
| 自然景观（山/洞） | 多方向斜切（间隔 30°） | 逐层、横（意义不大） | 无正交主导方向 |
| 室内多层 | 每层楼板横切 + 楼梯斜切 | — | 层间关系靠横切比对 |
| 大场景（村庄级） | L0 概览 + 单栋 L1 | 整体 L1/L2 | 先保证全局合理，再逐栋细查 |

**示例输出（南墙垂直截面，10 列 × 12 层，规划破损 35%）：**
```
层11  ▓▓···▓·······     ← 顶部大缺口（符合规划）
层10  ▓▓··▓▓·······
层09  ▓▓▓··▓▓▓···▓·
层08  ▓▓▓▓···▓▓▓▓▓·
层07  ▓▓▓▓▓▓▓▓▓▓▓▓·     ← 底部完整
...
层00  ▓▓▓▓▓▓▓▓▓▓▓▓▓     ← 落地闭合，无悬空
      列0→→→→→→→→列12
角部检查：列0 每层都有 ▓ → 与西墙闭合 ✓
错缝检查：层02 列3 与层03 列3 同列 → 违反错缝 ✗（需修正）
```

**示例输出（对角斜截面，验证四角闭合 + 后殿衔接）：**
```
        ←西北角          中心           东北角→
层11  ▓······×···▓··········▓······        角部 ▓ 保留
层07  ▓▓▓▓··▓▓▓▓▓▓▓▓··▓▓▓▓▓▓▓▓▓▓·        中段后殿鼓座块
层00  ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓        全线落地
断言：两端列 12 层全 ▓ → 西北/东北角闭合 ✓
     层07 中段 ▓ 连续 → 后殿与中殿墙体衔接 ✓
```

## 为什么比截图探针更适合 Agent
- 截图像素受光照/雾/抗锯齿影响，颜色断言有误报；截面读的是**构建期真实数据**，零噪声
- 截图只能验证"渲染出来什么样"，截面能定位到**具体哪块砖**错位
- 截图只有相机一个视角；截面可任意方向、任意粒度切，覆盖相机看不到的死角
- 纯文本输出，任何模型都能读，无需图像处理能力
- 不需要渲染管线存在（构建期即可跑），CI 环境友好

**实现要点**：

> ⚠️ **当前 chapel 项目状态**：`BlockInfo` 记录 + `slice()` 工具函数尚未落地实现。方案 D 是方法论级别的自查设计，下个场景或本项目迭代时建议优先补上（侵入极小，砌墙时 push 一条记录即可）。落地前，几何断言（方案 A）+ 人工截图自查仍是实际的验证闭环。

```rust
// 构建期顺手记录（侵入最小）
struct BlockInfo { x: f32, y: f32, z: f32, sx: f32, sy: f32, sz: f32, planned_hole: bool }
// 墙生成器放置每块砖时 push 一条

// 切截面：任意平面（点 p0 + 法向 n），网格分辨率 res
// 把每块砖 AABB 投影到切面 uv 基 → box-raster 到网格 → 字符矩阵
fn slice(blocks: &[BlockInfo], p0: Vec3, n: Vec3, res: f32) -> CharGrid;

// 预制切法
fn slice_horizontal(blocks: &[BlockInfo], y: f32, res: f32) -> CharGrid;      // 横
fn slice_vertical_along(blocks: &[BlockInfo], axis: Vec3, at: f32) -> CharGrid; // 竖
fn slice_diagonal(blocks: &[BlockInfo], dir: Vec3) -> CharGrid;               // 斜（对角/任意）
```

**E. 审美近似（无法量化的部分）**
```
颜色直方图：饱和度分布（玩具感=高饱和占比过高）
           明度分布（过曝=高光区堆积，死黑=暗部堆积）
仍需人工或 VLM 最终把关，结构性问题已被 A-D 覆盖
```

### 5.3 自查清单（全部通过才交付）
- [ ] 整体布局合理（L0 概览截面：主体/附属结构关系、朝向）
- [ ] 所有墙角闭合（竖截面两端列 + 斜截面角部：每层均为 ▓）
- [ ] 砖缝错缝正确（逐层截面：相邻层同列不连续 ▓）
- [ ] 无穿模（AABB 断言：屋顶/门/地面）
- [ ] 无悬空砖块（逐层截面：▓ 下方有支撑或落地）
- [ ] 破损分布与规划表一致（截面缺口 × 标记 = 规划破损位）
- [ ] 斜构件合理（斜截面：屋顶坡度/坍塌面走向）
- [ ] 参照物（树/草/碎砖）已放置
- [ ] 色调映射正常（高光不死白、阴影不死黑；无视觉时用颜色直方图近似）

---

## 第 6 阶段：交付与迭代

1. **交付物**：截图 + 本轮改动说明 + 已自查项列表
2. **大改前询问**：用户说"不对劲"时，先问具体哪项（颜色/布局/破损），不要盲目重做
3. **小步迭代**：每轮只改一类问题（先结构后材质），改完重新截图
4. **沉淀配方**：完成一个场景后，把可复用部分（色板/图元工具/墙生成器）抽到 `geoms.rs` 类工具文件，下个场景直接复用

---

## 常见故障速查表

| 症状 | 根因 | 修复 |
|------|------|------|
| 砖块对不齐 | 手写魔法坐标 | 常量 + 公式推导（2.1） |
| 墙面不闭合 | 角点不共享 | ±W/2 推导 + 角柱补缝（2.3） |
| 玩具感强 | 高饱和色 + 无色调映射 + 无参照物 | 三管齐下（第 3、4 阶段 + 参照物） |
| 画面过曝发白 | 缺 tonemapping / 相机缺 Hdr 组件 | Hdr（bevy::camera::Hdr 独立组件） + Tonemapping::AgX（第 4 阶段） |
| 画面死黑 | 环境光缺失或太弱 | GlobalAmbientLight 资源提亮 |
| 物体"漂浮" | 阴影未开启 | shadow_maps_enabled: true（0.19 已改名，不是 shadows_enabled） |
| 整帧泛白雾感 | 雾色用了天空蓝 | 雾用暖灰调 |
| 破损看着假 | 均匀随机撒洞 | 缺损集中顶部 + 碎砖靠墙脚（2.5） |
| 编译报 API 不存在 | Bevy 版本 API 变动 | 见附录 B |
| 链接失败 wayland 等 | 缺系统依赖 | 见附录 A |

---

## 附录 A：环境准备（Linux 沙箱）

```bash
# Bevy 0.18 系统依赖
apt install libwayland-dev libxkbcommon-dev libudev-dev libasound2-dev

# 无 GPU 时软渲染
LIBGL_ALWAYS_SOFTWARE=1 cargo run

# Cargo.toml 编译提速（dev 也要能跑）
[profile.dev.package."*"]
opt-level = 3
[profile.dev]
opt-level = 1
```

**版本兼容**：默认 Bevy 0.19（需 rustc ≥1.95）。沙箱/CI 环境若 rustc <1.95 则降级到 Bevy 0.18，API 差异详见附录 B。

## 附录 B：Bevy 0.18→0.19 API 变动清单（实测踩坑 + warbell 0.19.1 对照）

> **源头权威**：warbell 项目 `Cargo.toml` 固定 `bevy = "0.19.1"`，`src/scene.rs` 是视觉管线源头，所有写法已与它对齐。chape​l 项目已验证编译通过。

| 变动项 | 0.18 / 旧写法 | 0.19 正确写法（warbell 验证） | 备注 |
|------|------|------|------|
| **Hdr 组件路径** | `bevy::render::view::Hdr` 或老写法 `Camera { hdr: true }` | `use bevy::camera::Hdr;`，作为独立组件 spawn | 0.17 把 hdr 从 Camera 字段拆出来，0.19 又把模块从 render::view 移到 camera |
| **平行光阴影开关** | `shadows_enabled: true` | `shadow_maps_enabled: true` | 编译时会有类似字段提示，但容易漏改 |
| **DistanceFog 路径** | （0.18 同路径，但很多老教程写错到 bevy::render::fog 等） | `use bevy::pbr::{DistanceFog, FogFalloff};` | warbell `scene.rs` L18-L19 权威 |
| **Exposure 路径** | `bevy::pbr::Exposure` 或写在 Camera 字段里 | `use bevy::camera::Exposure;`，作为独立组件 spawn | 与 Hdr 一起移到 camera 模块 |
| **Tonemapping** | `bevy::render::camera::Tonemapping` | `use bevy::core_pipeline::tonemapping::Tonemapping;` | 独立组件，不加会过曝 |
| **Color 构造** | `Color::rgb(1,1,1)` ❌ 不存在 | `Color::srgb(1.0, 0.96, 0.88)` 或 `Color::srgb_u8(0xb9, 0xb5, 0xa8)` | 0.18 已是如此，0.19 没变，但新手指南常写错 |
| **Mesh 导入** | 老路径 `bevy::render::mesh::Mesh` | `use bevy::mesh::Mesh;` | 0.18 已是如此，0.19 没变 |
| **NotShadowCaster 路径** | 各种乱路径 | `use bevy::light::NotShadowCaster;` | warbell `vista.rs` L16 验证 |
| **GlobalAmbientLight** | 0.18 前是 AmbientLight 组件 | `.insert_resource(GlobalAmbientLight { color, brightness, ..default() })` | 是 Resource，不是组件；缺 brightness=画面死黑 |
| **Resources-as-Components** | `#[derive(Resource, Component)]` 合法 | **不再允许**：Resource 是 Component 子 trait；一个类型不能同时双义 | 拆成 `MyDataComp(Component)` + `MyDataRes(Resource)` 两个类型 |
| **bevy_scene 改名** | `bevy_scene` crate | 重命名为 `bevy_world_serialization`，代码里仍 `use bevy::scene::*`（prelude 重导出了名字不变） | 影响 Cargo.toml 单独引用场景 crate 的情况；prelude 正常用户无感 |
| **edition 2024** | crate 可 edition=2021 | warbell 用 `edition = "2024"`（但 chapel 用 2021 也没问题） | 0.19 要求 rustc ≥1.95；2024 edition 不是强制项 |

## 附录 C：提示词模板（复制给下一个 Agent 即用）

```
参考 <已验证项目/仓库> 的渲染管线与建模配方，搭建 <场景>。

流程要求：
1. 先输出建造规划（等我确认再写代码）：
   - ASCII 平面图 + 尺寸常量表
   - 每面墙/部件的破损等级表
   - 砖块规格与错缝规则
   - 低饱和色板（每材质3档明暗）
   - 参照物清单
2. 坐标一律由常量公式推导，禁止手写小数坐标
3. 变化与破损用确定性哈希（可复现）
4. 共享白色材质+顶点色合批，平面着色
5. HDR + AgX 色调映射 + 暖阳 + 阴影 + 环境光
6. 完成后执行自查清单（墙角闭合/砖缝/穿模/悬空/破损一致）：
   - 无视觉能力时，用内存截面扫描做断言：横/竖/斜多方向切面 +
     粗略→细致分级粒度（L0 概览 → L1 标准 → L2 局部细查），
     并依据场景类型选择切面组合（矩形建筑横竖为主、圆/斜构件加斜切、
     大场景先 L0 全局再逐栋 L1）；几何断言作快速前置
   - 像素/深度探针仅在截面方案不可行时使用
   - 截图仅用于交付展示给人看，不作为模型自查依据
7. 交付：截图 + 改动说明；信息不全时先问我
```

## 附录 D：参考实现索引

| 文件 | 作用 | 0.19 关键 API（避坑） |
|------|------|------|
| `src/geoms.rs` | 调色板 + 图元 + 合并/着色工具（可复用配方库） | `merged_flat()` 合批配方：tinted → merge → duplicate_vertices → compute_flat_normals |
| `src/chapel.rs` | 布局常量、coursed_wall 砌墙器、确定性破损、碎砖 | `hash2()` 确定性哈希；drop_frac 控制「缺损集中顶部」 |
| `src/env.rs` | 地面、草地、碎石、树（参照物） | `build_tree()` 的 warbell 风格低模树：frustum 树干 + 两层 cone 树冠 |
| `src/main.rs` | 应用骨架、光照管线、轨道相机 | **0.19 必须看**：`bevy::camera::Hdr`、`shadow_maps_enabled`、`bevy::pbr::DistanceFog` — 都是 0.19 新写法 |
| warbell `src/scene.rs` | **视觉管线源头**（AgX/雾/环境光/阴影/IBL/Bloom） | 权威导入行（L6-L27）：<br>`use bevy::camera::{Exposure, Hdr};`<br>`use bevy::pbr::{AtmosphereSettings, ContactShadows, DistanceFog, FogFalloff, SSAO};`<br>`use bevy::light::{Atmosphere, CascadeShadowConfigBuilder, ...}` |
| warbell `src/meshkit.rs` | **合批配方源头** | 与 chapel `geoms.rs` 思路一致：primitive → tint → merge → flat shade → 单白色材质 |
| warbell `Cargo.toml` | Bevy 版本锚点 | `bevy = "0.19.1"`（不是浮动 0.19，固定小版本防 break） |
| warbell `docs/specs/bevy-0-18-1-polished-static-3d-scene-verified-apis.md` | 0.18 管线验证文档 | 0.19 大部分可复用，但注意附录 B 的 API 改名表 |
